import { ColibriResizeObserverDirective } from './colibri-resize-observer.directive';

describe('ColibriResizeObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new ColibriResizeObserverDirective();
    expect(directive).toBeTruthy();
  });
});
