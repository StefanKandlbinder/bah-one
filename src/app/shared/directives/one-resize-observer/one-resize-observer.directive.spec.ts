import { OneResizeObserverDirective } from './one-resize-observer.directive';

describe('oneResizeObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new OneResizeObserverDirective();
    expect(directive).toBeTruthy();
  });
});
